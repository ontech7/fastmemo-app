// Native (iOS/Android) stub: there is no Tauri runtime, so these are inert.
// Keeps `@tauri-apps/api` out of the React Native bundle entirely.

export const isTauri = false;

export async function closeTauriSplashscreen(): Promise<void> {
  // no-op
}
